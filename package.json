{
  "name": "lost-limb-riders-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "generate:compliance": "node scripts/generate-compliance-manifest.mjs",
    "generate:documents": "node scripts/generate-document-manifest.mjs",
    "generate:onboarding-references": "node scripts/generate-onboarding-references.mjs",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.1",
    "@vercel/blob": "^2.8.0"
  }
}
